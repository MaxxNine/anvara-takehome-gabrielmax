# Challenge 02 - Conversion Tracking

- [x] Promote `apps/frontend/lib/analytics.ts` into `apps/frontend/lib/analytics/` with `core.ts`, `events.ts`, `hooks.ts`, and `index.ts`.
- [x] Preserve the existing `@/lib/analytics` import path and Challenge 01 tracking API compatibility during the refactor.
- [x] Extend the typed analytics event system with `page_view`, `marketplace_view`, `placement_request_submit`, `placement_request_success`, `form_submit`, and `form_error`.
- [x] Add typed event parameter builders for ad slots and campaigns.
- [x] Add `useTrackPageView()` for SPA route changes.
- [x] Add `useTrackOnMount()` for client-side view events when useful.
- [x] Create `apps/frontend/app/components/analytics-provider.tsx`.
- [x] Wrap the root layout with `AnalyticsProvider` so SPA page views are tracked globally.
- [x] Track `marketplace_view` on marketplace load.
- [x] Enrich `ad_slot_click` with structured ad slot params.
- [x] Enrich `ad_slot_view` with structured ad slot params and `conversion_type: 'micro'`.
- [x] Track `placement_request_submit` and `placement_request_success` with `conversion_type: 'macro'`.
- [x] Track `form_submit` and `form_error` in sponsor campaign form.
- [x] Track `form_submit` and `form_error` in publisher ad slot form.
- [x] Verify `page_view` fires on client-side route changes.
- [x] Verify `marketplace_view`, `ad_slot_click`, `ad_slot_view`, `placement_request_submit`, and `placement_request_success`.
- [x] Verify `form_submit` and `form_error`.
- [x] Run `pnpm --filter @anvara/frontend typecheck`.
- [x] Run `pnpm --filter @anvara/frontend lint`.
